# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
itsdangerous>=2.1.0
starlette>=0.36.0

# ── HTTP clients (ESI calls + relay REST) ────────────────────────────────────
# requests: sync HTTP used in game_eve.py, eve.py, and direct ESI helpers
requests>=2.31.0
aiohttp>=3.9.0
httpx>=0.27.0
websockets>=12.0

# ── Image processing (nebula DDS → JPEG conversion) ──────────────────────────
Pillow>=10.0.0

# ── Auth / crypto ─────────────────────────────────────────────────────────────
cryptography>=42.0.0
# bcrypt: constant-time password hashing (dev bypass code, C-1)
bcrypt>=4.0.0
# PyJWT[crypto]: local JWKS JWT validation for EVE SSO tokens (L-4)
# [crypto] extra pulls in cryptography RSA/EC key support (already a dep above)
PyJWT[crypto]>=2.8.0

# ── Desktop window + tray icon ───────────────────────────────────────────────
pywebview>=5.0.0
pystray>=0.19.0

# ── Build (dev only — not shipped in the executable) ─────────────────────────
# pip install pyinstaller
